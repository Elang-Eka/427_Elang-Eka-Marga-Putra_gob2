Summary sesi 1
427-Elang Eka Marga Putra

=> Pengertian web service yaitu penghubung atau jembatan antara frontend dan backend yang biasa disebut dengan REST API
=> Intall go and Introduction
=> Create project go 
=> belajar tentang variable yang terdiri dari : 
   	1.variable with data type
   	2.variable without data type
   	3.multiple variable
   	4.underscore variable
=> belajar data type yaitu :
      1.int (integer)
      2.float
      3.string
      4.bool/boolean
      5.nil and zero value
=> belajar constant and operator  :
   	1.Constant (nilai yang tidak bisa dirubah)
   	2.Aritmetic operators (
		tambah(+), kurang(-), bagi(/), kali(*), modulus(%), increment(++), decrement(--)
        )
   	3.Relational Operators (
            lebih dari(>), kurang dari(<), lebih dari sama dengan(>=), kurang dari sama dengan(<=)
            sama dengan (==), tidak sama dengan(!=)
        )
  	4.Logical Operators(
            AND(&&), OR(||), NOT(!)
        )